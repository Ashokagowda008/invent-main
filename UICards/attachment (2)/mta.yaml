_schema-version: "3.2"
ID: attachment
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: attachment-destination-content
  type: com.sap.application.content
  requires:
  - name: attachment-destination-service
    parameters:
      content-target: true
  - name: attachment-repo-host
    parameters:
      service-key:
        name: attachment-repo-host-key
  - name: attachment-uaa
    parameters:
      service-key:
        name: attachment-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: attachment_html_repo_host
          ServiceInstanceName: attachment-html5-srv
          ServiceKeyName: attachment-repo-host-key
          sap.cloud.service: attachment
        - Authentication: OAuth2UserTokenExchange
          Name: attachment_uaa
          ServiceInstanceName: attachment-xsuaa-srv
          ServiceKeyName: attachment-uaa-key
          sap.cloud.service: attachment
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: attachment-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: attachment-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - attachment.zip
      name: attachment
      target-path: resources/
- name: attachment
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: attachment-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: attachment-destination-service
    service-plan: lite
- name: attachment-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: attachment-xsuaa-srv
    service-plan: application
- name: attachment-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: attachment-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
